<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
        <item name="colorPrimary">@color/colorTheme</item><!--Appbar背景色-->
        <item name="colorPrimaryDark">@color/colorTheme</item><!--状态栏颜色-->
        <item name="colorAccent">@color/colorTheme</item><!--控制各个控件被选中时的颜色-->
        <item name="toolbarStyle">@style/toolbar</item>
    </style>

    <style name="toolbar" parent="Base.Widget.AppCompat.Toolbar">
        <item name="android:background">@color/colorTheme</item>
    </style>

    <style name="no_background_dialog" parent="@android:style/Theme.Dialog">
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:windowFrame">@null</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowIsFloating">true</item>
        <item name="android:windowFullscreen">false</item>
        <item name="android:backgroundDimEnabled">false</item>
    </style>

    <style name="dialog_text">
        <item name="android:textColor">@color/dialog_text_color</item>
        <item name="android:textAlignment">gravity</item>
        <item name="android:gravity">center</item>
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">@dimen/video_input_dialog_item_height</item>
    </style>

    <style name="dialog_divider">
        <item name="android:layout_width">@dimen/video_input_dialog_divider_width</item>
        <item name="android:layout_height">@dimen/video_input_dialog_divider_height</item>
        <item name="android:layout_gravity">center_horizontal</item>
        <item name="android:background">@color/colorGRAY</item>
    </style>

</resources>
